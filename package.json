{
  "name": "online-radio",
  "version": "1.0.0",
  "description": "Full-featured online radio manager and streaming platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "nuxt:install": "cd nuxt && npm install",
    "nuxt:dev": "cd nuxt && npm run dev",
    "nuxt:build": "cd nuxt && npm run build",
    "build": "cd nuxt && npm install && npm run build"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.16.12",
    "cors": "^2.8.5",
    "ejs": "^6.0.1",
    "express": "^4.18.2",
    "ffmpeg-static": "^5.2.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "socket.io": "^4.7.2",
    "sql.js": "^1.11.0",
    "uuid": "^9.0.0",
    "web-push": "^3.6.7",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.8",
    "utf-8-validate": "^6.0.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
